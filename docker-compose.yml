version: "3.9"

# Nori — backend-only stack (Podman on VPS)
#
# Services:
#   backend  → FastAPI + Discord bot on port 12000
#   bullmq   → rotates Groq/Tavily/Exa keys every 5 min
#
# Redis runs natively on the VPS at 127.0.0.1:6379 with ACL auth

services:

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: nori-backend
    ports:
      - "12000:12000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - REDIS_HOST=host.docker.internal
      - REDIS_PORT=6379
      - REDIS_USER=your_redis_username
      - REDIS_PASSWORD=your_redis_password
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    networks:
      - nori-net

  bullmq:
    build:
      context: .
      dockerfile: Dockerfile.bullmq
    container_name: nori-bullmq
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - REDIS_HOST=host.docker.internal
      - REDIS_PORT=6379
      - REDIS_USER=your_redis_username
      - REDIS_PASSWORD=your_redis_password
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    networks:
      - nori-net

networks:
  nori-net:
    driver: bridge